name: text_transformation_animation
description: A text transformation animation.
version: 0.1.3
homepage: https://github.com/dhshah/FlutterTextTransformationAnimationWidget

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
